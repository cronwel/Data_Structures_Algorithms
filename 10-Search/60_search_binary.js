function bsearch(arr, num){
  const l = [];
  const r =[];
  for(var = x; x < arr.length; x++){
    // if(arr[x] > )
  }
};

// THis is my rambling initial attempt to try to code something that resmebles the way it might work
